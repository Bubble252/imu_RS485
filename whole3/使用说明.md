# 部署B

```bash
./deploy_to_target_server.sh

```

# ssh到服务器并建立通道
```bash
ssh -i ./capri_yhx_2237 -p 2237     -o "ProxyCommand ssh -i ./leo_tmp_2258.txt -p 2258 -W %h:%p root@202.112.113.78"     -L 5555:localhost:5555     -L 5556:localhost:5556     -L 5557:localhost:5557     -L 5558:localhost:5558     root@202.112.113.74
```
# 打开虚拟环境&运行B
```bash
conda activate lerobot_server

cd /data7/yhx/lerobot_data_collection

./start_b_whole.sh

```

# A本地运行
```bash
python A_real_video.py

```

# C本地运行
```bash
python C_real_video_reverse_ultra.py
```