# PyQt5 UI 环境说明

## 🎯 专用conda环境

已为PyQt5 UI创建了独立的conda环境 `pyqt5_ui`，完全隔离，避免与lerobot环境冲突。

## 📦 环境信息

**环境名称**: `pyqt5_ui`  
**Python版本**: 3.10.19  
**已安装包**:
- PyQt5 (5.15.11) - GUI框架
- pyqtgraph (0.14.0) - 高性能绘图
- PyOpenGL (3.1.10) - 3D渲染
- pyzmq (27.1.0) - 网络通信
- opencv-python-headless (4.12.0.88) - 图像处理（无GUI冲突）
- numpy (2.2.6) - 数值计算

## 🚀 使用方法

### 方法1：自动启动（推荐）

启动脚本会自动检测并激活pyqt5_ui环境：

```bash
cd /home/bubble/桌面/WIT_RS485/pyqt5_viewer
./start_viewer.sh
```

### 方法2：手动激活

```bash
# 激活环境
conda activate pyqt5_ui

# 启动UI
cd /home/bubble/桌面/WIT_RS485/pyqt5_viewer
python imu_dual_cam_viewer.py
```

### 方法3：指定远程主机

```bash
./start_viewer.sh --host 192.168.1.100 --port 5560
```

## 🔧 环境管理

### 查看环境信息
```bash
conda activate pyqt5_ui
conda list
```

### 更新包
```bash
conda activate pyqt5_ui
pip install --upgrade PyQt5 pyqtgraph PyOpenGL
```

### 删除环境（如果需要）
```bash
conda deactivate
conda remove -n pyqt5_ui --all -y
```

### 重新创建环境
```bash
conda create -n pyqt5_ui python=3.10 -y
conda activate pyqt5_ui
pip install PyQt5 pyqtgraph PyOpenGL pyzmq opencv-python-headless numpy
```

## 🎯 为什么需要独立环境？

### 问题背景
lerobot环境中同时安装了`opencv-python`和`opencv-python-headless`，导致Qt插件冲突：
```
qt.qpa.plugin: Could not load the Qt platform plugin "xcb"
```

### 解决方案
创建专用环境pyqt5_ui：
- ✅ 只安装`opencv-python-headless`（无Qt插件）
- ✅ PyQt5使用系统Qt库（无冲突）
- ✅ 完全隔离，互不影响

## 📊 环境对比

| 特性 | lerobot环境 | pyqt5_ui环境 |
|------|------------|--------------|
| 用途 | 主程序（IMU数据采集） | PyQt5 UI显示 |
| opencv | opencv-python + opencv-python-headless | opencv-python-headless |
| Qt冲突 | ❌ 有冲突 | ✅ 无冲突 |
| PyQt5 | 5.15.11 | 5.15.11 |
| 通信 | ZMQ发布端（5560） | ZMQ订阅端（5560） |

## 🔄 完整工作流程

### Terminal 1: 主程序（lerobot环境）
```bash
conda activate lerobot
cd /home/bubble/桌面/WIT_RS485

python triple_imu_rs485_publisher_dual_cam_UI.py \
    --online-only \
    -p /dev/ttyUSB1 \
    --b-host localhost --b-port 5555 \
    --enable-video --video-host localhost --video-port 5557 \
    --enable-debug --debug-port 5560
```

### Terminal 2: PyQt5 UI（pyqt5_ui环境）
```bash
cd /home/bubble/桌面/WIT_RS485/pyqt5_viewer
./start_viewer.sh
```

## 💡 常见问题

### Q1: 启动脚本没有自动激活环境？
**A**: 手动激活后再启动：
```bash
conda activate pyqt5_ui
python imu_dual_cam_viewer.py
```

### Q2: 提示"环境不存在"？
**A**: 重新创建环境（见上方"重新创建环境"）

### Q3: 还是出现Qt插件错误？
**A**: 确认当前环境：
```bash
conda env list  # 查看当前环境（*标记）
conda activate pyqt5_ui  # 确保激活正确环境
```

### Q4: 想要在base环境运行？
**A**: 可以，但需要确保base环境也安装了依赖：
```bash
conda deactivate  # 退出所有conda环境
pip3 install PyQt5 pyqtgraph PyOpenGL pyzmq opencv-python numpy
./start_viewer.sh
```

## 📝 验证环境

运行以下命令验证环境配置正确：

```bash
conda activate pyqt5_ui
python -c "
import sys
print('Python:', sys.version)
import PyQt5
print('PyQt5:', PyQt5.Qt.PYQT_VERSION_STR)
import pyqtgraph
print('pyqtgraph:', pyqtgraph.__version__)
import zmq
print('pyzmq:', zmq.zmq_version())
import cv2
print('opencv:', cv2.__version__)
import numpy
print('numpy:', numpy.__version__)
print('✓ 所有依赖正常')
"
```

预期输出：
```
Python: 3.10.19 ...
PyQt5: 5.15.11
pyqtgraph: 0.14.0
pyzmq: 27.1.0
opencv: 4.12.0
numpy: 2.2.6
✓ 所有依赖正常
```

---

**✅ 环境已就绪，可以开始使用了！**
